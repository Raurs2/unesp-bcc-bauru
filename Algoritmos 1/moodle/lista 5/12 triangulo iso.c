#include <stdio.h>
#include <math.h>
int main() {
    int i, j, d = 1, max, b;
    scanf("%d", &max);
    for (i = 1; i < max; i++)
    {
        for (b = 0; b < ; b++)
        {
            printf("   ");
        }
        
        for (j = 1; j <= d; j++)
        {
            printf(" * ");
        }
        printf("\n");
        d += 2;
        
    }
    
    return 0;
}
#include <stdio.h>
#include <stdlib.h>

#define max 100

int main()
{
    int i, x, y, ini, j, k, xy;
    int m[max], n[max], vetor[maxX];

    do
    {
        scanf("%d", &x);

    } while (n != 0);
    return 0;
}

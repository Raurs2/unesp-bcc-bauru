#include<stdio.h>

int main()
{
    int x0, y0, x, y, tot, k, route, i, j, xy0[][], xy[][], r[][];
    do
    {
        scanf("%d %d", &x0, &y0);
        if (x0 == 0 && y0 == 0) break;

        printf("%d\n\n", tot);
    } while (1);

    
    
    return 0;
}

#include<stdio.h>

void insertPaciente()
{
    
}

int main()
{
    char nome[30], estado[20], pacientes[1000];
    while (scanf("%s %s", nome, estado) != EOF)
    {
        
    }
    
    return 0;
}

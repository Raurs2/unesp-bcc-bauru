#include <bits/stdc++.h>

#define MAX 1001

using namespace std;

int main()
{
    int tests;
	cin	>> tests;

    while (tests--)
    {
        int n, m, k, s, t;
        cin	>> n >> m >> k >> s >> t;

        int d[MAX], c[MAX], l[MAX];
        for (int i = 0; i < m; i++)
            cin >> d[i] >> c[i] >> l[i];

        int u[MAX], v[MAX], q[MAX];
        for (int i = 0; i < k; i++)
            cin >> u[i] >> v[i] >> q[i];
        
    }
    

    return 0;
}